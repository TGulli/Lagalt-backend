package com.noroff.lagalt.user.model;

/**
 * Lagalt internal way of keeping track of which login method
 * users are created with
 */

public enum LoginMethod {
    internal,
    facebook,
    google
}
